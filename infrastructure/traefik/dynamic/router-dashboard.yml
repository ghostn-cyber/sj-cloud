# Traefik Dashboard Router Configuration
http:
  routers:
    traefik-dashboard:
      entryPoints:
        - websecure
      rule: "Host(`traefik.sj-cloud.test`) || Host(`traefik.platform.test`)"
      service: api@internal
      tls: {}
      middlewares:
        - dashboard-auth
        - security-headers
